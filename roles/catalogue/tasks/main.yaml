  -  name: copy mongod repo
     ansible.builtin.copy:
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo
  - name: install mongodb client
    ansible.builtin.dnf:
     name: mongodb-mongosh
     state: installed

  - name: check products load or not
    ansible.builtin.shell: mongosh --host 172.31.66.86 --eval 'db.getMongo().getDBNames().indexOf("catalogue")'
    register: catalogue_output

  - name: print the output
    ansible.builtin.debug:
      msg: "{{ catalogue_output }}"

  - name: load the products
    ansible.builtin.shell: mongosh --host 172.31.66.86 </app/db/master-data.js
    when: catalogue_output.stdout | int < 0


     
